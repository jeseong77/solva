import ProblemFormScreen from "@/components/ProblemFormScreen";

export default function AddProblemRoute() {
  // Renders the component, does not pass a problemId prop.
  return <ProblemFormScreen />;
}
